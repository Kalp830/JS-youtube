let index = 0;
while (index <= 10) {
    //console.log(`Value of index is ${index}`);
    index = index + 2
}

// While with arry
let myArry = ["TVS", "Honda", "Bajaj", "Hero"];
let newArry = 0;
 while (newArry < myArry.length) {
   // console.log(`Value is ${myArry[newArry]}`);
    newArry = newArry + 1
 }

 // Do While loop
 let score = 1;
 do {
     console.log(`Score is ${score}`);
     score++
 } while (score <= 10);